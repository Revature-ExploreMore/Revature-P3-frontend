export interface User {
    id: number,
    name: string,
    username:string,
    email: string,
    phoneNumber: string,
    password: string,
    darkModePreference: boolean,
    registerDate:Date,
    roleId: number,
}
